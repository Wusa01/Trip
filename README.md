# سجل العشيرة الرقمي — Digital Tribe Family Registry System

نظام رقمي متكامل لتوثيق أفراد عشيرة كبيرة: بياناتهم، علاقاتهم العائلية (آباء/أبناء/زواج)،
صورهم، وأنسابهم عبر أجيال غير محدودة، مع دعم البحث والشجرة التفاعلية وتقارير PDF عربية.

---

## المتطلبات التقنية

- Python 3.10+
- Flask 3.x + Flask-SQLAlchemy + Flask-Login
- SQLite (قابل للاستبدال لاحقاً بـ PostgreSQL دون تعديل الكود، فقط `DATABASE_URL`)
- HTML / CSS / JavaScript (بدون إطار عمل فرونت إند، Cytoscape.js لعرض الشجرة)
- reportlab + arabic-reshaper + python-bidi لتوليد تقارير PDF عربية بخط Amiri

---

## هيكل المشروع

```
tribe_registry/
├── run.py                      # نقطة تشغيل التطبيق
├── config.py                   # الإعدادات (تُقرأ من .env)
├── .env.example                # قالب متغيرات البيئة — انسخه باسم .env واملأه
├── requirements.txt
│
├── app/
│   ├── __init__.py             # Application Factory + تسجيل الـ Blueprints + ترحيل تلقائي للقاعدة
│   ├── extensions.py           # db, login_manager
│   ├── models.py               # Person, Relation, Closure, Marriage, Tribe, User, AuditLog
│   │
│   ├── services/
│   │   ├── relation_service.py # منطق العلاقات، اشتقاق العكس، بناء جدول الإغلاق (Closure)
│   │   ├── person_service.py   # إنشاء/تعديل الأشخاص، رفع الصور، البحث، الخصوصية
│   │   ├── tribe_service.py    # إدارة العشائر والفروع (قائمة، إضافة، تعديل، حذف)
│   │   ├── tree_service.py     # الفرع المحلي للشجرة، تقرير النسب، تقرير الفرع
│   │   ├── auth_service.py     # إنشاء أول حساب، تعديل الحساب الشخصي، إعادة تعيين كلمات المرور
│   │   ├── email_service.py    # إرسال البريد عبر Gmail SMTP (رمز التفعيل، النسخ الاحتياطية)
│   │   └── backup_service.py   # نسخ احتياطي بالبريد، نسخة تلقائية قبل الاستيراد، الاستيراد
│   │
│   ├── routes/
│   │   ├── auth_routes.py      # إعداد أول حساب، دخول/خروج، حسابي، إدارة المستخدمين، نسخ/استيراد
│   │   ├── person_routes.py    # إضافة/تعديل/بحث/عرض بطاقة شخص
│   │   ├── relation_routes.py  # إضافة علاقات، اعتماد/رفض، زواج/طلاق
│   │   ├── tree_routes.py      # API الشجرة والتقارير (JSON)
│   │   ├── report_routes.py    # تحميل تقارير PDF
│   │   └── tribe_routes.py     # شاشة إدارة العشائر والفروع (للمدير فقط)
│   │
│   ├── utils/
│   │   └── pdf_generator.py    # توليد PDF عربي بخط Amiri (بطاقة، نسب، فرع)
│   │
│   ├── templates/               # قوالب Jinja2 (RTL بالكامل)
│   └── static/
│       ├── css/style.css        # الهوية البصرية (حبر/ورق/نحاسي، خطا Amiri وCairo)
│       ├── fonts/                # ⚠️ ضع هنا Amiri-Regular.ttf وAmiri-Bold.ttf يدوياً
│       ├── js/                   # tree.js, person_form.js, setup.js, profile.js, ...
│       └── uploads/              # صور الأفراد (تُنشأ تلقائياً)
│
└── instance/
    ├── tribe.db                  # قاعدة البيانات (تُنشأ تلقائياً)
    └── auto_backups/             # نسخ تلقائية تُؤخذ قبل كل عملية استيراد
```

---

## التثبيت والتشغيل

```bash
# 1. تثبيت المكتبات
pip install -r requirements.txt

# 2. تحميل خط Amiri ووضعه في app/static/fonts/
#    Amiri-Regular.ttf و Amiri-Bold.ttf
#    المصدر: https://fonts.google.com/specimen/Amiri

# 3. إعداد البريد الإلكتروني (لازم لرمز التفعيل وإرسال النسخ الاحتياطية)
cp .env.example .env
# ثم افتح .env وضع فيه:
#   MAIL_USERNAME=بريد Gmail الكامل
#   MAIL_APP_PASSWORD=كلمة مرور تطبيقات من 16 خانة
# طريقة إنشائها:
#   1) فعّل "التحقق بخطوتين" من myaccount.google.com/security
#   2) اذهب إلى myaccount.google.com/apppasswords وأنشئ كلمة مرور جديدة
#   3) انسخها في MAIL_APP_PASSWORD (وليس كلمة مرور Gmail العادية)

# 4. تشغيل النظام
python run.py
```

افتح المتصفح على `http://127.0.0.1:5000` — بما أنه لا يوجد أي حساب بعد، ستظهر لك تلقائياً **صفحة إنشاء أول حساب مدير**:
1. أدخل اسم المستخدم، البريد الإلكتروني، وكلمة المرور
2. سيصلك رمز تفعيل من 6 أرقام على البريد خلال ثوانٍ
3. أدخل الرمز لإتمام إنشاء الحساب — ستدخل مباشرة إلى المنصة

> ⚠️ **تسجيل الدخول إلزامي فعلياً لكل الصفحات.** نسخة سابقة من `app/__init__.py`
> كانت تحتوي على خاصية `auto_login` تُسجّل دخول أي زائر تلقائياً كأول حساب مدير
> دون كلمة مرور — تم حذفها لأنها كانت تُبطل نظام الصلاحيات بالكامل.

### هل عندك نسخة بيانات سابقة (نقل لجهاز جديد)؟
أنشئ حساباً أولاً كما في الخطوات أعلاه (أي حساب، حتى لو مؤقت)، ثم من داخل المنصة:
**إدارة المستخدمين ← استيراد نسخة بيانات** — ارفع ملف `.db` القديم. سيُستبدل الحساب الذي أنشأته للتو (وكل البيانات) بمحتوى الملف المستورد بالكامل، وستحتاج بعدها لتسجيل الدخول بنفس بيانات الحساب الذي كان موجوداً وقت أخذ تلك النسخة.

---

## المبادئ الأساسية للنظام

- **سجل واحد لكل شخص:** لا تكرار — كل فرد يُدخل مرة واحدة، ويُربط بأبيه/أمه المُدخلين مسبقاً بدل إعادة إدخالهم.
- **أجيال غير محدودة:** يمكن إضافة جد أعلى أو حفيد جديد في أي وقت دون إعادة بناء الشجرة.
- **معرف عام (`public_id`)** لكل شخص لتمييز الأسماء المتكررة (شائعة جداً في عشيرة كبيرة)، إلى جانب حقل "ملاحظة تمييز" اختياري.
- **علاقات باتجاه واحد فقط** (`PARENT_OF`) في قاعدة البيانات؛ العلاقات العكسية (ابن/ابنة/أخ/أخت) تُشتق تلقائياً في الكود لتفادي التناقضات.
- **جدول إغلاق (Closure Table)** منفصل يُبنى تلقائياً عند اعتماد كل علاقة، لتسريع استعلامات النسب الكامل والفروع في عشيرة كبيرة دون الحاجة لاستعلامات متكررة بطيئة.
- **مراجعة واعتماد:** أي شخص أو علاقة يضيفها "الباحث" تدخل بحالة `pending` ولا تظهر في الشجرة أو التقارير حتى يعتمدها المدير.
- **سجل تدقيق (Audit Log):** كل إضافة/تعديل/اعتماد يُسجَّل بمن قام به ومتى.
- **خصوصية الأحياء:** الصورة وتاريخ الميلاد الدقيق للأشخاص الأحياء تظهر للمدير فقط.
- **تحميل تدريجي للشجرة:** عرض جيلين فقط للأعلى وللأسفل حول كل شخص، مع إمكانية التوسيع، بدل تحميل عشرات الآلاف من العقد دفعة واحدة.

---

## الأدوار والصلاحيات

| الدور | الصلاحيات |
|---|---|
| **مدير (admin)** | إضافة/تعديل/حذف/اعتماد كل شيء، إدارة المستخدمين |
| **باحث (researcher)** | إضافة أشخاص وعلاقات (تدخل بحالة "قيد المراجعة") |
| **مستخدم (user)** | البحث ومشاهدة البطاقات المعتمدة فقط |

إدارة العشائر والفروع (إضافة/تعديل/حذف) متاحة من صفحة **العشائر والفروع** في القائمة الجانبية، للمدير فقط.
لا يمكن حذف عشيرة مرتبط بها أفراد حالياً.

### إدارة الحساب

- **أي مستخدم** يمكنه تعديل اسم المستخدم/البريد/كلمة المرور الخاصة به من صفحة **"حسابي"** (تتطلب كلمة المرور الحالية لأي تغيير).
- **المدير فقط** يمكنه إعادة تعيين كلمة مرور أي مستخدم آخر نسي كلمة مروره، من صفحة **إدارة المستخدمين**، دون الحاجة لأي سكربت أو وصول لقاعدة البيانات.
- **المدير فقط** يمكنه إرسال نسخة كاملة من قاعدة البيانات بالبريد الإلكتروني، أو استيراد نسخة سابقة لاستبدال البيانات الحالية بالكامل (مع نسخة احتياطية تلقائية تُؤخذ أولاً قبل أي استيراد، تُحفظ في `instance/auto_backups/`).

---

## الهوية البصرية

مستوحاة من أرشيف/سجل رسمي وليست قالباً تسويقياً:
- **الألوان:** حبر أخضر-كُحلي داكن، ورق عتيق باهت، نحاسي مطفأ (لون الأختام)، أحمر طيني (لشارات الحالة فقط)
- **الخطوط:** Amiri للعناوين وأسماء الأفراد (نفس خط تقارير PDF)، Cairo للواجهة والبيانات
- **العنصر المميز:** شارة "ختم" دائرية بحدود منقّطة لحالة الاعتماد (معتمد / قيد المراجعة / مرفوض)

